skeynet-hive 设计简要:
	目前还没有任何实质性的逻辑改动, 只是做了:
		* 编写makefile
		* 带入不同版本的lua
		* 做了code style format 格式化, 提高可阅读性
			(不能调整struct 顺序来节约内存, io 读写操作, 可能使用了内存地址偏移, 一旦修改结构体的变量位置, 就容易内存崩溃, 不要动)
		* 对io event 事件模块进行了优化
		* 对所有宏常量进行了优化
		* 选定版本为lua 5.4.6, 暂时不支持5.1.5, 改回旧版本, 需要调整很多api;
			有些方法, 是旧版lua 没有的, 需要做转换写法, 比较麻烦;

	2023/12/31

	后续改动, 后续再添加;
